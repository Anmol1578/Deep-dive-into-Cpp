#include <iostream>
using namespace std;

int main()
{

    // Ternary operator ?: = replacement to an if/else statement

    // condition ? expression1 : expression2;

    /*   EXAMPLE 1
         int grade = 75;
         grade >= 60 ? cout << "You pass " : cout << "You fail ";        */

    // EXAMPLE 2
    int number = 9;
    number % 2 == 1 ? cout << "ODD" : cout << " ODD ";

    return 0;
}